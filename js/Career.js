define([
  'require', 'jquery',  'modernizr',  '../dataStore', '../DataSet',
], function (require) {

    var store = require('../dataStore');

    return {
        init : function () {
            this.getState();
        },
        eventXXX : function() {
            this.setState();
        },

        render() {

        }

    };
});